# ===========================================
# SOU ESPORTE - MOBILE ENVIRONMENT CONFIG
# ===========================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env:
#    cp .env.example .env
#
# 2. Descubra o IP do seu Mac/PC na rede local:
#    Mac:     ipconfig getifaddr en0
#    Windows: ipconfig | findstr IPv4
#    Linux:   hostname -I | awk '{print $1}'
#
# 3. Edite o .env e substitua SEU_IP pelo IP encontrado
#
# Exemplo: Se seu IP for 192.168.0.14, use:
#   EXPO_PUBLIC_API_URL=http://192.168.0.14:3000
#
# IMPORTANTE: O IP deve ser o mesmo configurado no backend (api/.env)
# ===========================================

# URL da API Backend (OBRIGATÓRIO)
# Altere SEU_IP para o IP real do seu computador
EXPO_PUBLIC_API_URL=http://SEU_IP:3000
